<p id="notice"><%= notice %></p>

<h1>ADMINS</h1>

<table class="table table-hover">
  <thead>

      <div id="main-container" class="container-fluid">
        <div class="row" style="border: 1px solid black;">
          <div class="col-xs-3">
    <tr>
      <th>USERNAME</th>
      <th>PASSWORD</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.Username %></td>
        <td><%= admin.Password %></td>
        <td><%= link_to 'Show', admin, class: 'btn btn-primary btn-xs' %></td>
        <td><%= link_to 'Edit', edit_admin_path(admin), class: 'btn btn-warning btn-xs' %></td>
        <td><%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-xs' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
    </div>
      <div class="col-xs-9">

      </div>
    </div>
    <%= link_to 'New Admin', new_admin_path, class: 'btn btn-success btn-xs'  %>
    <%= link_to 'New Teacher', new_teacher_path, class: 'btn btn-success btn-xs'  %>
    <%= link_to 'New Course', new_course_path, class: 'btn btn-success btn-xs'  %>
    <%= link_to 'New Student', new_student_path, class: 'btn btn-success btn-xs'  %> <br /> <br />
    <%= link_to 'New Cohort', new_cohort_path, class: 'btn btn-success btn-xs'%>
    <%= link_to 'New Course', new_course_path, class: 'btn btn-success btn-xs'  %>
    <%= link_to 'New Teacher Cohort', new_teacher_cohort_path, class: 'btn btn-success btn-xs'  %>
    <%= link_to 'New Student Cohort', new_student_cohort_path, class: 'btn btn-success btn-xs'  %>

  </div>

</body>

<br>
